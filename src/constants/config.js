// js/config/config.js

const CONFIG = {
    GRID: {
        MAJOR_COLOR: "#ccc",
        MINOR_COLOR: "#eee",
        SIZE: 60, // 1 foot = 60 pixels
        QUARTER_SIZE: 15
    },
    CANVAS: {
        DEFAULT_WIDTH: 4500,
        DEFAULT_HEIGHT: 4500,
        TEXT_SIZE: 10
    },
    ZOOM: {
        MIN: 0.3,
        MAX: 3,
        STEP: 0.1
    },
    PIXELS_PER_FOOT: 60
}; 
